#include "main.h"
/**
 * _abs - - description: calculate the absolute value of a whole number
 * @x: absolute value
 * Return: x basolute value
 */

int _abs(int x)
{
	if (x < 0)
	{
	x = x * -1;
	}

	return (x);
}
