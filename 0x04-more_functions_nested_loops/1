#include "main.h"
/**
 * print_line - - print a line
 * Return:_
 */
void print_line(int n)
{
	int a;
	int _;
	
	if(n <= 0)
		_putchar(10);
	else
		{
		for(a = n; a > n; a++) 
			_putchar(_);
		}
	_putchar(10);
}

