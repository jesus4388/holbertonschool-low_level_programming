#include "main.h"
/**
 * print_most_numbers -- d
 * Return: a
 */
int print_most_numbers(void)
{
	int a = 0;

	if (a != 2 && a != 4)
	{
	for (a = 0; a <= 9; a++)
	_putchar(a);
	_putchar('\n');
	}
	return (a);
}
