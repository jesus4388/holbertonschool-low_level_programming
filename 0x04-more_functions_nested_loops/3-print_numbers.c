#include "main.h"
/**
 * print_numbers - descripiton: function to print the number
 * Return: a
 */
void print_numbers(void)
{
	int a = 0;

	for (a = '0'; a <= '9'; a++)
	_putchar(a);
	_putchar('\n');
}
