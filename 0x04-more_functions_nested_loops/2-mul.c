#include "main.h"
/**
 * mul - - description: Function to multiply two integers.
 * @c: the result of both integers
 * @a:first integer
 * @b: second interger
 * Return: the result of both integers
 */
int mul(int a, int b, int c)
{
	int c = 0;

	c = (a * b);
	return (c);
}
