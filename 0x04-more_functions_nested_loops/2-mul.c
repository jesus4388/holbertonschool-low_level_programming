#include "main.h"
/**
 * mul - description: Function to multiply two integers.
 * @a: first integer
 * @b: second interger
 * Return: the result of both integers
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
